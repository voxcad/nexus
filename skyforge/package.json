{
  "name": "skyforge",
  "version": "0.1.0",
  "private": true,
  "description": "Drone data processing SaaS — photos in, deliverables out",
  "scripts": {
    "dev": "pnpm --filter @skyforge/web dev",
    "dev:api": "pnpm --filter @skyforge/api dev",
    "build": "pnpm --filter @skyforge/web build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=22"
  }
}
